services:
  app:
    build: .
    container_name: smartkb
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./uploads:/app/uploads
      - ./app.db:/app/app.db
    env_file:
      - .env
    restart: unless-stopped
